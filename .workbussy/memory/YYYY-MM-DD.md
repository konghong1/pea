2026-07-27: Re-wrote NodePromptInput.tsx with comprehensive fix for @ reference deletion bug. Key changes: (1) handleInput now checks isCursorInsideOrAdjacentToToken at the very beginning and clears atTriggerRef if cursor near a token, preventing stale trigger state; (2) tightened @ trigger condition to only fire when distance === 0 (cursor exactly at '@'), eliminating false re-trigger after typing+deleting; (3) Backspace handling cleans up atTriggerRef on token deletion. This fully addresses the issue where deleting mistyped text after @ image reference would accidentally delete the referenced image.

2026-07-27: Updated selected node outer ring — changed from subtle gray to brand color (#1fa2dc) using system token `--pea-brand` for better visibility across light/dark modes. Added scale animation. Fixed Delete key removal bug via capture phase listener + existence guard in `CanvasEditor.tsx`.
