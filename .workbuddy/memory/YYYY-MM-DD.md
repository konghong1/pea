2026-07-27: Fixed text node drag bug in PeaNode.tsx - Modified onMouseDown handler on contentEditable div to allow drag start recording while preventing browser auto-fix. Single-click now correctly selects text nodes for dragging; double-click still enters edit mode. Also fixed NodeChatPrompt disappearing by caching anchor element with ref, improved cursor inheritance with CSS (`* { cursor: inherit }`), and added hover lift effect (transform translateY + shadow). TypeScript builds successfully after all changes.
